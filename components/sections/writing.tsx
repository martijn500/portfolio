"use client";

// Writing was moved into `components/sections/community.tsx`.
// This file is kept for compatibility but intentionally renders nothing to
// avoid duplicate content/IDs. Remove when all imports are updated.

export default function Writing() {
  return null;
}
